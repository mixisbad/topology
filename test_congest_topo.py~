"""Custom topology example

Two directly connected switches plus a host for each switch:

   host --- switch --- switch --- host

Adding the 'topos' dict with a key/value pair to generate our newly defined
topology enables one to pass in '--topo=mytopo' from the command line.
"""

from mininet.topo import Topo
from mininet.link import TCLink

class MyTopo( Topo ):
    "Simple topology example."

    def __init__( self ):
        "Create custom topo."

        # Initialize topology
        Topo.__init__( self )

        # Add hosts and switches
	h1 = self.addHost( 'h1' )
        h2 = self.addHost( 'h2' )
        h3 = self.addHost( 'h3' )

	h4 = self.addHost( 'h4' )
        h5 = self.addHost( 'h5' )
        h6 = self.addHost( 'h6' )


	s1 = self.addSwitch( 's1' )
	s2 = self.addSwitch( 's2' )
	s3 = self.addSwitch( 's3' )
        s4 = self.addSwitch( 's4' )


        # Add links
        self.addLink( h1, s1, bw=5, delay='1ms', loss=0 )
        self.addLink( h2, s1, bw=5, delay='1ms', loss=0 )
        self.addLink( h3, s1, bw=5, delay='1ms', loss=0 )


	self.addLink( h4, s4, bw=5, delay='1ms', loss=0 )
        self.addLink( h5, s4, bw=5, delay='1ms', loss=0 )
        self.addLink( h6, s4, bw=5, delay='1ms', loss=0 )

	self.addLink( s1, s2, bw=5, delay='1ms', loss=0 )
	self.addLink( s1, s3, bw=5, delay='1ms', loss=0 )
        self.addLink( s1, s4, bw=5, delay='1ms', loss=0 )

        self.addLink( s2, s4, bw=5, delay='1ms', loss=0 )

	self.addLink( s3, s4, bw=5, delay='1ms', loss=0 )

topos = { 'mytopo': ( lambda: MyTopo() ) }
